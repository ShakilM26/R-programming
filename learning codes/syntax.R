# Syntax 

# Variable
# In R, there are three ways to write variables

var1 = "Shakil"
var2 <- "Mou"
"Shakil & Mou" -> var3

print(var1)
print(var2)
print(var3)

# Why does the output begin with [1]? In R, each line of the answer begins with [i] indicating that 
# the first element of the object displayed on that line is the 1st element of that object. 